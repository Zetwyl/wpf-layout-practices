<Window x:Class="Task_3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task_3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel Margin="0,30,0,0">
            <Slider x:Name="SliderFontSize" Minimum="0" Maximum="50" Value="25" />

            <TextBox Text="{Binding ElementName=SliderFontSize, Path=Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <TextBlock FontSize="{Binding ElementName=SliderFontSize, Path=Value}"
                       Text="Пример WflF-приложения для демонстрации привязки данных" />
        </StackPanel>
        
    </Grid>
</Window>
