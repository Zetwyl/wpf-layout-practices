<Window x:Class="Task_3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Task_3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <Style x:Key="SmallTextStyle" TargetType="TextBox">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>

        <Style BasedOn="{StaticResource SmallTextStyle}" TargetType="TextBox" x:Key="LargeTextStyle">
            <Setter Property="FontSize" Value="30" />
            <Setter Property="Height" Value="150" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" x:Name="LeftPanel" Background="AliceBlue">
            <!-- Одно поле большое по умолчанию, остальные маленькие -->
            <TextBox Style="{StaticResource LargeTextStyle}" GotFocus="TextBox_Focus"/>
            <TextBox Style="{StaticResource SmallTextStyle}" GotFocus="TextBox_Focus"/>
        </StackPanel>

        <StackPanel Grid.Column="1" x:Name="RightPanel" Background="AliceBlue">
            <TextBox Style="{StaticResource SmallTextStyle}" GotFocus="TextBox_Focus"/>
            <TextBox Style="{StaticResource SmallTextStyle}" GotFocus="TextBox_Focus"/>
        </StackPanel>
    </Grid>

</Window>